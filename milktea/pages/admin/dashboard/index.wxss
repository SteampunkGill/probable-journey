.dashboard-container {
  display: flex;
  flex-direction: column;
  gap: 40rpx;
  padding: 32rpx;
  background: linear-gradient(135deg, #f5f0e1 0%, #f8f4e6 100%);
  min-height: 100vh;
}

.metrics-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
}

.metric-card {
  background: linear-gradient(135deg, #fff8dc 0%, #f5f0e1 100%);
  padding: 32rpx 24rpx;
  border-radius: 40rpx;
  box-shadow: 0 8rpx 24rpx rgba(160, 82, 45, 0.08);
  border: 4rpx solid #e8dccb;
  position: relative;
  overflow: hidden;
}

.metric-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6rpx;
  background: linear-gradient(90deg, #ffc0cb, #a0522d);
}

.metric-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.metric-label {
  color: #4a3b30;
  font-weight: 600;
  font-size: 28rpx;
}

.metric-unit {
  background: rgba(160, 82, 45, 0.1);
  color: #a0522d;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
}

.metric-value {
  font-size: 48rpx;
  font-weight: 700;
  color: #8b4513;
  margin-bottom: 24rpx;
}

.metric-footer {
  font-size: 24rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.compare-label {
  color: #7a6a5b;
  opacity: 0.8;
}

.compare-value {
  padding: 4rpx 12rpx;
  border-radius: 20rpx;
  font-weight: 600;
}

.compare-value.up {
  background: rgba(44, 150, 120, 0.1);
  color: #2c9678;
}

.compare-value.down {
  background: rgba(255, 107, 107, 0.1);
  color: #ff6b6b;
}

.card {
  background: linear-gradient(135deg, #fff8dc 0%, #f5f0e1 100%);
  border-radius: 40rpx;
  padding: 32rpx;
  box-shadow: 0 8rpx 24rpx rgba(160, 82, 45, 0.08);
  border: 4rpx solid #e8dccb;
}

.card-header {
  font-size: 32rpx;
  font-weight: 700;
  margin-bottom: 32rpx;
  color: #8b4513;
  padding-bottom: 16rpx;
  border-bottom: 4rpx dashed #d4c7b5;
}

.charts-row {
  display: flex;
  flex-direction: column;
  gap: 32rpx;
}

.bar-chart {
  height: 400rpx;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  padding-top: 60rpx;
  position: relative;
}

.bar-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

.bar-wrapper {
  width: 40rpx;
  height: 280rpx;
  background: rgba(222, 184, 135, 0.1);
  border-radius: 20rpx;
  position: relative;
  display: flex;
  align-items: flex-end;
  border: 2rpx solid #e8dccb;
  overflow: hidden;
}

.bar {
  width: 100%;
  background: linear-gradient(to top, #a0522d 0%, #d2b48c 100%);
  border-radius: 20rpx;
  transition: all 0.4s;
}

.bar-label {
  font-size: 20rpx;
  color: #7a6a5b;
}

.line-chart-placeholder {
  height: 300rpx;
  position: relative;
  border-bottom: 4rpx dashed #d4c7b5;
  border-left: 4rpx dashed #d4c7b5;
  margin: 20rpx;
}

.line-point {
  position: absolute;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.point {
  width: 16rpx;
  height: 16rpx;
  background: #a0522d;
  border-radius: 50%;
  border: 4rpx solid #fff8dc;
}

.point-label {
  font-size: 18rpx;
  color: #8b4513;
  margin-top: 8rpx;
  transform: rotate(-45deg);
  white-space: nowrap;
}

.bottom-row {
  display: flex;
  flex-direction: column;
  gap: 32rpx;
}

.ranking-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.ranking-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx;
  border-radius: 30rpx;
  background: rgba(255, 255, 255, 0.7);
  border: 2rpx solid #e8dccb;
}

.rank-info {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.rank-index {
  width: 40rpx;
  height: 40rpx;
  background: rgba(222, 184, 135, 0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: #7a6a5b;
  font-weight: 700;
}

.rank-index.top3 {
  background: #a0522d;
  color: #fff8dc;
}

.rank-name {
  font-size: 28rpx;
  color: #4a3b30;
  font-weight: 600;
}

.rank-data {
  display: flex;
  gap: 16rpx;
  font-size: 24rpx;
}

.rank-count {
  color: #2c9678;
}

.rank-amount {
  color: #8b4513;
}

.alert-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.alert-item {
  padding: 24rpx;
  border-radius: 30rpx;
  border: 4rpx solid;
}

.alert-item.warning {
  background: rgba(255, 192, 203, 0.1);
  border-color: rgba(255, 192, 203, 0.3);
}

.alert-item.danger {
  background: rgba(255, 107, 107, 0.1);
  border-color: rgba(255, 107, 107, 0.3);
}

.alert-title {
  font-weight: 700;
  font-size: 28rpx;
  margin-bottom: 8rpx;
}

.alert-desc {
  font-size: 24rpx;
  color: #7a6a5b;
}

.empty-alert {
  text-align: center;
  color: #d2b48c;
  padding: 60rpx 0;
  font-size: 28rpx;
}