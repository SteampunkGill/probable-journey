<view class="search-page">
  <!-- 搜索栏 -->
  <view class="search-header">
    <view class="search-input-wrapper">
      <input 
        type="text" 
        placeholder="搜索奶茶、咖啡、小食..." 
        value="{{searchKeyword}}"
        bindinput="onInput"
        bindconfirm="onSearch"
        focus="{{true}}"
      />
      <view class="close-icon" wx:if="{{searchKeyword}}" bindtap="clearKeyword">✕</view>
    </view>
    <view class="search-action" bindtap="onSearch">
      <text class="search-icon">🔍</text>
    </view>
  </view>

  <!-- 搜索历史 -->
  <view class="search-history" wx:if="{{!showResults && searchHistory.length > 0}}">
    <view class="history-header">
      <text class="history-title">搜索历史</text>
      <text class="delete-icon" bindtap="clearHistory">🗑️</text>
    </view>
    <view class="history-tags">
      <view 
        class="history-tag" 
        wx:for="{{searchHistory}}" 
        wx:key="*this"
        bindtap="searchByKeyword"
        data-keyword="{{item}}"
      >
        {{item}}
      </view>
    </view>
  </view>

  <!-- 热门搜索 -->
  <view class="hot-search" wx:if="{{!showResults}}">
    <view class="hot-header">
      <text class="hot-title">热门搜索</text>
    </view>
    <view class="hot-tags">
      <view 
        class="hot-tag {{index < 3 ? 'hot' : ''}}" 
        wx:for="{{hotKeywords}}" 
        wx:key="word"
        bindtap="searchByKeyword"
        data-keyword="{{item.word}}"
      >
        {{index + 1}}. {{item.word}}
      </view>
    </view>
  </view>

  <!-- 搜索建议 -->
  <view class="search-suggest" wx:if="{{showSuggest && searchKeyword && !showResults}}">
    <view class="suggest-list">
      <view 
        class="suggest-item" 
        wx:for="{{searchSuggest}}" 
        wx:key="*this"
        bindtap="searchByKeyword"
        data-keyword="{{item}}"
      >
        <text class="suggest-icon">🔍</text>
        <text class="suggest-text">{{item}}</text>
      </view>
    </view>
  </view>

  <!-- 搜索结果 -->
  <view class="search-results" wx:if="{{showResults}}">
    <!-- 结果类型切换 -->
    <view class="search-tabs">
      <view
        class="tab-item {{searchType === 'product' ? 'active' : ''}}"
        bindtap="switchTab"
        data-type="product"
      >商品 ({{filteredProducts.length}})</view>
      <view
        class="tab-item {{searchType === 'store' ? 'active' : ''}}"
        bindtap="switchTab"
        data-type="store"
      >门店 ({{filteredStores.length}})</view>
    </view>

    <!-- 商品搜索结果 -->
    <block wx:if="{{searchType === 'product'}}">
      <view class="results-header">
        <view class="sort-options">
          <view
            class="sort-option {{sortBy === 'default' ? 'active' : ''}}"
            bindtap="changeSort"
            data-sort="default"
          >综合</view>
          <view
            class="sort-option {{sortBy === 'sales' ? 'active' : ''}}"
            bindtap="changeSort"
            data-sort="sales"
          >销量</view>
          <view
            class="sort-option {{sortBy === 'price_asc' ? 'active' : ''}}"
            bindtap="changeSort"
            data-sort="price_asc"
          >价格 ↑</view>
          <view
            class="sort-option {{sortBy === 'price_desc' ? 'active' : ''}}"
            bindtap="changeSort"
            data-sort="price_desc"
          >价格 ↓</view>
        </view>
      </view>

      <view class="product-list" wx:if="{{filteredProducts.length > 0}}">
        <view class="product-grid">
          <product-card
            wx:for="{{filteredProducts}}"
            wx:key="id"
            product="{{item}}"
            bindtap="onProductTap"
            data-id="{{item.id}}"
          />
        </view>
      </view>
      <view class="empty-results" wx:else>
        <view class="empty-icon">🔍</view>
        <view class="empty-text">没有找到相关商品</view>
      </view>
    </block>

    <!-- 门店搜索结果 -->
    <block wx:else>
      <view class="store-list" wx:if="{{filteredStores.length > 0}}">
        <view
          class="store-item"
          wx:for="{{filteredStores}}"
          wx:key="id"
          bindtap="onStoreTap"
          data-id="{{item.id}}"
        >
          <view class="store-info">
            <view class="store-name">{{item.name}}</view>
            <view class="store-address">{{item.address}}</view>
            <view class="store-status">
              <text class="status-tag {{item.status}}">{{item.status === 'OPEN' ? '营业中' : '休息中'}}</text>
              <text class="distance" wx:if="{{item.distance}}">{{item.distance}}km</text>
            </view>
          </view>
          <view class="store-action">
            <text class="go-btn">去下单</text>
          </view>
        </view>
      </view>
      <view class="empty-results" wx:else>
        <view class="empty-icon">🏪</view>
        <view class="empty-text">没有找到相关门店</view>
      </view>
    </block>
  </view>
</view>