spring:
  datasource:
    url: jdbc:mysql://mysql:3306/milktea_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 13603994106
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
      hibernate.format_sql: true
  flyway:
    enabled: false

jwt:
  secret: "your-secret-key-that-is-at-least-32-bytes-long-here!"
  expiration: 86400000

websocket:
  allowed-origins: "*"
  endpoint: /ws
  broker-prefix: /topic
  app-destination-prefix: /app

logging:
  level:
    com.milktea.app: DEBUG
    org.springframework.security: DEBUG

file:
  upload-dir: /app/uploads
  max-size-mb: 5
  supported-image-types: image/jpeg,image/png,image/gif